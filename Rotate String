class Solution {
public:
    bool rotateString(string s, string goal) {
        string newString = s + s;
        if(s.size() != goal.size()){
            return false;
        }
        if(newString.find(goal) == -1){
            return false;
        }else{
        return true;
        }
    }
};
